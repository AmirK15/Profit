import React from 'react';

export const App = () => {
  const sadf = 'fds';

  return (
    <div style={{ height: '100vh', display: 'flex', justifyContent: 'center', alignItems: 'center' }}>Profit App</div>
  );
};
